const modal=document.querySelector('#modal');document.querySelectorAll('.form-open').forEach(x=>x.onclick=()=>modal.showModal());document.querySelector('.close').onclick=()=>modal.close();modal.onclick=e=>{if(e.target===modal)modal.close()};document.querySelector('#form').onsubmit=e=>{e.preventDefault();e.currentTarget.innerHTML='<p style="font:700 29px/1 Barlow Condensed,sans-serif">Есть. В реальном проекте заявка ушла бы менеджеру клуба.</p>'};
new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in')}}),{threshold:.15}).observe;const observer=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('in')),{threshold:.15});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
document.querySelectorAll('.program').forEach(button=>button.onclick=()=>{document.querySelectorAll('.program').forEach(x=>x.classList.remove('active'));button.classList.add('active')});
const visual=document.querySelector('[data-parallax]');visual.addEventListener('pointermove',e=>{const r=visual.getBoundingClientRect();visual.style.transform=`rotate(2.5deg) translate(${((e.clientX-r.left)/r.width-.5)*7}px,${((e.clientY-r.top)/r.height-.5)*7}px)`});visual.addEventListener('pointerleave',()=>visual.style.transform='rotate(2.5deg)');
